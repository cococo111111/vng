package com.vng.zing.credits.report.mail;

import java.text.NumberFormat;
import java.util.Locale;

/**
 *
 * @author quytp2@vng.com.vn
 */
public class CreditsMail {
    public static void main(String[] args) throws InterruptedException {
        /*
         * Gen pass: System.out.println(SecurityHelper.md5("password" +
         * "hash"));
         */        
//        NumberFormat nf = NumberFormat.getInstance(Locale.GERMAN);     
//        System.out.println(nf.format(1111111111));
        /*
         * Test auto report AutoDailyReporter.monitor(); while (true) { try {
         * Thread.sleep(3600); } catch (InterruptedException ex) { }
        }
         */
    }
}
